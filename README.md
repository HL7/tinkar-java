# Tinkar
## Running locally
Follow these steps to install and run _Tinkar_ locally.

```sh
git clone https://github.com/HL7/tinkar-java.git

# install dependencies first
cd jpms-deps
mvn clean install

# install Tinkar
cd ../tinkar-core
mvn clean install
...
```
